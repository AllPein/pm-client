import styled from "styled-components";

const FieldName = styled.h2`
  margin: 1.2rem 0;
  font-size: 1.5rem;
  font-weight: 400;
  color: ${(props) => props.theme.color.primary4};
`;

export { FieldName };
